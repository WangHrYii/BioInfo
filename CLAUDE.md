# CLAUDE.md

## 项目上下文

每次会话开始时，先读取以下文件了解项目状态：

1. 读取 `.claude-summary.md` 了解项目概述、技术栈、目录结构
2. 读取 `.tasks/current.md` 了解当前任务状态和进度
3. 读取 `OUTLINE.md` 了解完整教程大纲（写章节内容时参考）

## 项目关键信息

- 这是一个 **Docsify 静态文档网站**，不需要构建步骤
- 本地预览：`docsify serve .`（端口 3000，支持 LiveReload）
- 所有教程内容在 `docs/` 和 `appendix/` 目录下，每章一个 .md 文件
- 章节文件命名规则：`{编号}-{英文短名}.md`，如 `09-rnaseq.md`
- 大纲参考：`OUTLINE.md` 包含所有章节的完整结构（1500+ 行）
- 导航菜单：`_sidebar.md`，新增/删除章节时需同步更新

## 写作规范

- 目标读者：零基础小白，语言通俗易懂，多用比喻
- 每个概念先解释"是什么"、"为什么重要"，再讲"怎么做"
- 代码示例必须可直接运行
- 图表使用 Mermaid 语法（Docsify 已配置支持）
- 不要在正文中堆砌术语，专业术语首次出现时给出中文解释
